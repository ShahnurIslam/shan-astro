<figure class="decision-preview pricing-preview">
	<svg viewBox="0 0 500 190" role="img" aria-labelledby="pricing-preview-title pricing-preview-desc">
		<title id="pricing-preview-title">Demand controlled by a pricing intervention</title>
		<desc id="pricing-preview-desc">Observed demand rises toward an intervention point. Without intervention, forecast demand exceeds usable capacity. After a twelve percent pricing intervention, projected demand finishes at capacity.</desc>
		<line class="pricing-preview__capacity" x1="20" y1="70" x2="480" y2="70" />
		<text class="pricing-preview__capacity-label" x="20" y="57">Usable capacity</text>
		<path class="pricing-preview__observed" d="M 20 171 C 92 164, 151 149, 198 129 C 220 119, 236 110, 250 104" />
		<path class="pricing-preview__without" d="M 250 104 C 310 75, 378 42, 470 22" />
		<path class="pricing-preview__after" d="M 250 104 C 317 86, 393 72, 470 70" />
		<circle class="pricing-preview__point" cx="250" cy="104" r="7" />
		<g class="pricing-preview__labels">
			<text class="pricing-preview__without-label" x="470" y="15" text-anchor="end">Without intervention</text>
			<text class="pricing-preview__after-label" x="470" y="98" text-anchor="end">After pricing intervention</text>
			<text class="pricing-preview__intervention-label" x="238" y="136" text-anchor="end">
				<tspan x="238">Pricing intervention</tspan>
				<tspan class="pricing-preview__price" x="238" dy="19">+12%</tspan>
			</text>
		</g>
	</svg>
</figure>

<style>
	.decision-preview {
		margin: 0;
		padding: clamp(.75rem, 2vw, 1.25rem);
		border: 1px solid var(--site-border);
		background: rgba(255, 255, 255, .3);
	}
	.pricing-preview svg {
		display: block;
		width: 100%;
		height: auto;
		overflow: visible;
	}
	.pricing-preview path {
		fill: none;
		stroke-linecap: round;
	}
	.pricing-preview__capacity {
		stroke: #8b7652;
		stroke-width: 2;
	}
	.pricing-preview__capacity-label,
	.pricing-preview__labels {
		fill: var(--site-blue);
		font-family: var(--font-atkinson), ui-sans-serif, sans-serif;
		font-size: 17px;
		font-weight: 700;
		letter-spacing: .01em;
	}
	.pricing-preview__capacity-label {
		fill: #6c5a3e;
	}
	.pricing-preview__observed {
		stroke: var(--site-accent);
		stroke-width: 6;
	}
	.pricing-preview__without {
		stroke: var(--site-accent);
		stroke-width: 4;
		stroke-dasharray: 10 9;
	}
	.pricing-preview__after {
		stroke: var(--site-blue);
		stroke-width: 6;
	}
	.pricing-preview__point {
		fill: var(--site-surface);
		stroke: var(--site-accent);
		stroke-width: 4;
	}
	.pricing-preview__without-label {
		fill: var(--site-accent);
	}
	.pricing-preview__after-label {
		fill: var(--site-blue);
	}
	.pricing-preview__intervention-label {
		font-size: 16px;
	}
	.pricing-preview__price {
		fill: var(--site-accent);
	}
	@media (max-width: 520px) {
		.decision-preview { padding: .6rem; }
		.pricing-preview__capacity-label,
		.pricing-preview__labels { font-size: 19px; }
		.pricing-preview__intervention-label { font-size: 18px; }
	}
</style>
